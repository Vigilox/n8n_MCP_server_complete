# Server configuration
PORT=3000            # Port where the MCP server will run
HOST=localhost       # Host address to bind the server to
NODE_ENV=development # Environment (development, production, test)

# n8n API configuration
# Make sure your n8n instance is running at this URL
N8N_BASE_URL=http://localhost:5678/api
# Generate an API key in your n8n instance settings and paste it here
N8N_API_KEY=your_n8n_api_key_here

# Logging
LOG_LEVEL=info      # Options: error, warn, info, debug 